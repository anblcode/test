<%= link_to 'Back', posts_path %> | <%= link_to 'Edit', edit_post_path(@post) %>
<p>
	<strong>Title:</strong>
	<%= @post.title %>
</p>
<p>
	<strong>Text:</strong>
	<%= @post.text %>
</p>

<h2>Comentarios</h2>
<%= render @post.comments %>


<%= render "comments/form" %>
